# Copy this file to .env only when a future application documents a variable here.
# Never commit credentials or production values.

# API
PORT=3000
WEB_ORIGIN=http://localhost:5173

# Local infrastructure only. Replace every credential outside local development.
POSTGRES_DB=trevvos_fieldops
POSTGRES_USER=fieldops
POSTGRES_PASSWORD=fieldops_local_only
POSTGRES_PORT=5432
POSTGRES_HOST=127.0.0.1
REDIS_PORT=6379
RABBITMQ_DEFAULT_USER=fieldops
RABBITMQ_DEFAULT_PASS=fieldops_local_only
RABBITMQ_AMQP_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
